[project]
name = "zenvy"
version = "0.1.0"
description = "Zenvy AI Voice Lab"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "python-dotenv",
    "psycopg2-binary",
    "redis",
    "python-multipart"
]

[tool.vercel]
entrypoint = "web.app:app"